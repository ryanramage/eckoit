beforeEach(function() {
  this.templates = _.extend(this.templates || {}, {
    thing: '<a href="#thing/{{id}}"{{#done}} class="done"{{/done}}>' +
            '<h2>{{name}}</h2>' +
            '<input class="edit" type="text" ' +
            'value="{{name}}" style="display:none"/>' + 
          '</a>' +
          '<a href="#" class="edit">Edit</a>'
  });
});

